// Q8. A shopkeeper wants to calculate the total bill amount after applying a 20% discount on the purchase. 
// Implement a solution to accept item no., quantity, and unit price. Compute the amount and apply 20% discount.
#include <stdio.h>

int main()
{
    int item_no, quantity;
    float unit_price;
    printf("Enter the item number: ");
    scanf("%d", &item_no);
    printf("Enter the quantity: ");
    scanf("%d", &quantity);
    printf("Enter the unit price: ");
    
    return 0;
}